import { FocusDetailsDirective } from './focus-details.directive';

describe('FocusDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
